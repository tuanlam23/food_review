<section class="light-bg booking-details_wrap">
  <div class="container">
    <div class="row justify-content-center">
      <%= render "users/user_left" %>
      <div class="col-8">
        <div class="row list user-follow-list">
          <div class="col-sm-12 pt-2">
            <% if @restaurants%>
              <div class="row" id="list_res">
              <% @restaurants.each do |restaurant|%>
                  <div class="col-sm-6 col-md-4 col-lg-4 featured-responsive">
                    <div class="featured-place-wrap">
                      <a href="<%= restaurant_path(restaurant)%>">
                        <img src="<%= restaurant.picture.url if restaurant.picture? %>" class="img-fluid img-product" alt="#">
                        <div class="featured-title-box f-content">
                          <a href="<%= restaurant_path(restaurant) %>" class="shop-title">
                            <%= restaurant.name %>
                          </a>
                          <ul>
                            <li>
                              <p>
                                <% (1..5).each do |number| %>
                                    <% star = restaurant.number_star >= number ? 'fa-star' : 'fa-star-o'%>
                                    <i class="fa <%= star %>"></i>
                                <% end %>
                              </p>
                            </li>
                            <li>
                              <span class="icon-location-pin"></span>
                              <p><%= restaurant.address + ", " + restaurant.area.district%></p>
                            </li>
                          </ul>
                          <div class="items-review">
                            <div class="avatar">
                              <a href="#">
                                <img class="img-profile rounded-circle img-profile" src="/images/featured1.jpg">
                              </a>
                            </div>
                            <div class="users-content block-with-text">
                              <a href="#" target="_blank"><b>Nam Nguyen</b></a>
                              <a href="" target="_blank">
                                TigerSugar là hãng trà sữa đầu tiên làm mình mê mẩn đến như thế!
                                Trân châu ở đây k giống như những chỗ khac đâu ạ.
                                Mềm dẻo cực kì, kết hợp với sữa tươi thì đỉnh luôn ấy! Rate 10/10 ❤️❤️🤤
                              </a>
                            </div>

                          </div>
                          <div class="items-review">
                            <span>3 Reviews</span>
                            <% if current_user.present? %>
                                <a href="#" class="follow <%= 'res-active' if Follow.check_follow(restaurant.id, current_user.id) %>" data-res="<%= restaurant.id %>" data-url="<%= follow_restaurants_path %>">
                                  <i class="fa fa-bookmark"></i>
                                  Lưu
                                </a>
                            <% end %>
                          </div>
                        </div>
                      </a>
                    </div>
                  </div>
              <% end %>

            </div>

              <div class="row">
                <div class="col-lg-12">
                  <%= will_paginate @restaurants, renderer: WillPaginate::ActionView::BootstrapLinkRenderer %>
                </div>
              </div>
            <% else %>
              <div class="row">
                <div class="col-12 mt-3" style="text-align: center">
                  <h6>Chưa lưu</h6>
                </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>